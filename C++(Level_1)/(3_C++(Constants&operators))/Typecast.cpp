#include <iostream>
using namespace std;

int main()
{
    float a = 98.26;
    cout << "The value of a:" << a << endl;
    cout << "The value of a:" << (int)a << endl;
    cout << "The value of a:" << (char)a << endl;
    //* This is called typecasting(converting one data-type into another.)
    /*
        (D.T)var = D.T(var)--->both are same.
        D.T var =(D.T)var; Genral syntax.
    */
}